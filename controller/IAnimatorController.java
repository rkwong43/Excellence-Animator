package cs3500.animator.controller;

/**
 * Represents a controller for an animator that keeps track of time.
 */
public interface IAnimatorController {

  /**
   * Runs the animation.
   */
  void startTimer();
}
